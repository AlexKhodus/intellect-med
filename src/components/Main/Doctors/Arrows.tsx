import React from 'react';
import './index.module.scss';

export const ArrowNext = () => {
    return (
        <div className="next"/>
    );
};

export const ArrowPrev = () => {
    return (
        <div className="prev"/>
    );
};
